<div id="<%= dom_id list %>">
  <p>
    <strong>List:</strong>
    <%= list.name %>
  </p>

</div>

<ul>
  <% list.todos.rank(:row_order).each do |todo| %>
    <li>
      <%= render todo %>  
    </li>
  <% end %>
</ul>
